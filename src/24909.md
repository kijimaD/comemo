# [インデックス 24909] ファイルの概要

このコミットは、Goランタイムにおけるスタックバリアの動作に関する高レベルな説明を `src/runtime/mstkbar.go` ファイルに追加するものです。この変更自体はコードの動作を変更するものではなく、主にドキュメンテーションの改善を目的としています。Goのガベージコレクタがどのようにスタックバリアを利用して、並行スキャンフェーズとストップ・ザ・ワールド（STW）マーク終了フェーズの間でゴルーチンのスタック変更を効率的に追跡するかについて、詳細な解説が加えられています。

## コミット

```
commit e9089e4ab6f5090092e416fa4d2c33d5279c0d72
Author: Austin Clements <austin@google.com>
Date:   Sat Aug 29 00:35:25 2015 -0400

    runtime: add high-level description of how stack barriers work
    
    Change-Id: I6affe75b5fa9dbf513c16200bff4fd7aa5f3a985
    Reviewed-on: https://go-review.googlesource.com/14051
    Reviewed-by: Rick Hudson <rlh@golang.org>
```

## GitHub上でのコミットページへのリンク

[https://github.com/golang/go/commit/e9089e4ab6f5090092e416fa4d2c33d5279c0d72](https://github.com/golang/go/commit/e9089e4ab6f5090092e416fa4d2c33d5279c0d72)

## 元コミット内容

このコミットは、`src/runtime/mstkbar.go` ファイルに、スタックバリアの機能、目的、実装の詳細、および関連する考慮事項について説明する extensive なコメントブロックを追加します。

## 変更の背景

Goのガベージコレクタ（GC）は、並行マーク＆スイープ方式を採用しており、プログラムの実行（ミューテータの動作）と並行してマークフェーズを実行します。しかし、並行マークフェーズ中にスタックが変更されると、GCは正確なオブジェクト到達可能性を判断するのが難しくなります。特に、並行スキャンフェーズでスタックをスキャンした後、STWマーク終了フェーズで再度スキャンする際に、スタックのどの部分が変更されたかを効率的に特定する必要があります。

スタックバリアは、この問題を解決するために導入されました。スタックバリアの導入により、GCはスタック全体を再スキャンするのではなく、変更された部分のみを再スキャンできるようになり、特に深いスタックを持つゴルーチンにおいて、STWポーズ時間を大幅に短縮することが可能になります。このコミットは、この重要なメカニズムの理解を深めるために、その高レベルな動作原理をドキュメントとして追加することを目的としています。

## 前提知識の解説

*   **Goのガベージコレクション (GC)**: GoのGCは、並行マーク＆スイープアルゴリズムをベースにしています。これは、プログラムの実行と並行して到達可能なオブジェクトをマークし、その後、到達不能なオブジェクトを解放するプロセスです。STW（Stop-The-World）フェーズは、GCが正確な状態を確保するために、一時的にすべてのゴルーチンの実行を停止する期間を指します。STWフェーズはGCのポーズ時間としてユーザーに知覚されるため、その時間を最小限に抑えることが重要です。
*   **スタック**: プログラムの実行中に、関数呼び出しやローカル変数の保存に使用されるメモリ領域です。Goでは、ゴルーチンごとに独自のスタックを持ち、必要に応じて動的にサイズが変更されます（スタックの拡大・縮小）。
*   **ポインタ**: メモリアドレスを指す変数です。GCは、ポインタをたどって到達可能なオブジェクトを特定します。
*   **ライトバリア (Write Barrier)**: GCが並行して動作している間に、ミューテータ（ユーザープログラム）がポインタを書き換える際に実行される特別なコードスニペットです。これにより、GCはオブジェクトグラフの変更を追跡し、誤って到達可能なオブジェクトを回収してしまうことを防ぎます。
*   **PC (Program Counter)**: 次に実行される命令のアドレスを保持するレジスタです。関数呼び出しでは、呼び出し元の関数の次の命令のアドレス（リターンPC）がスタックに保存され、関数が終了する際にそのアドレスに戻ります。

## 技術的詳細

スタックバリアは、Goのガベージコレクタが並行スキャンフェーズとSTWマーク終了フェーズの間でゴルーチンのスタックがどのように変化したかを効率的に追跡するためのメカニズムです。これにより、STWマーク終了フェーズでのスタックの再スキャンを、変更された部分のみに限定し、GCポーズ時間を短縮します。

**スタックバリアの動作原理:**

1.  **バリアの設置**: 並行スキャンフェーズ中にスタックがスキャンされる際、GCは特定のスタックフレームを選択し、そのフレームの保存されたリターンPC（プログラムカウンタ）を「スタックバリアトランポリン」のアドレスで上書きします。
2.  **バリアのヒット**: 後に、選択されたフレームがリターンする際、本来の呼び出し元に戻る代わりに、このスタックバリアトランポリンに「リターン」します。
3.  **トランポリンの処理**: トランポリンは、スタックがこのフレームを過ぎてアンワインドされたことを記録し、その後、スタックバリアが設置された際に記録しておいた元のリターンPCにジャンプして、実際の呼び出し元に処理を戻します。
4.  **マーク終了時の再スキャン**: STWマーク終了フェーズでは、GCはスタックを再スキャンしますが、スタックバリアにヒットしていない最初のフレームまでしかスキャンしません。これにより、変更されていないスタック部分は再スキャンが不要になります。また、ヒットしなかったスタックバリアは削除されます。

**軽量なメカニズム**:
このスキームは非常に軽量です。ミューテータ側でスタックのアンワインドを記録するための特別なコードは不要であり、トランポリンも数個のアセンブリ命令で構成されます。

**簿記 (Book-keeping)**:
スタックバリアの主なコストは、その簿記にあります。ランタイムは、すべてのスタックバリアの位置と元のリターンPCを記録する必要があります。これにより、バリアがヒットした際に正しい呼び出し元に戻ることができ、また、ヒットしなかったバリアを削除できます。このコストを最小限に抑えるため、Goランタイムは現在のフレームから1KB離れた位置から、指数関数的に間隔を空けてスタックバリアを配置します。これにより、簿記構造はスタックサイズに対して対数的に増加し、マーク終了時の再スキャンは必要量の最大2倍に抑えられます。
この簿記構造のためのスペースは、スタックアロケーション自体の最上部（最も外側のフレームのすぐ上）に予約されます。これは、通常のメモリ割り当て器がスタックを拡大する可能性があるため、スタック関連の構造を割り当てる際には使用できないためです。
デバッグ目的では、すべてのフレームにスタックバリアを設置することも可能ですが、これには大幅に多くの簿記スペースが必要です。

**正確性 (Correctness)**:
ランタイムとコンパイラは協力して、マーク終了時点でスタックから到達可能なすべてのオブジェクトがマークされることを保証します。

1.  **最低位の未ヒットスタックバリアより下のミューテータ書き込み**: 実行中の関数が自身のスタックフレームに書き込むすべての書き込みが含まれます。このスタック部分はマーク終了時に再スキャンされ、変更によって到達可能になったオブジェクトがマークされます。
2.  **最低位の未ヒットスタックバリアより上のミューテータ書き込み**: より上位のフレームが、自身のフレーム内のスタック変数へのポインタを渡した場合、ミューテータがスタックバリアより上のスタックを変更する可能性があります。これは「アップポインタ」と呼ばれます。コンパイラは、アップポインタを介した書き込みにライトバリアが付随することを保証します（アップポインタを介した書き込みとヒープポインタを介した書き込みを区別しません）。このライトバリアにより、変更によって到達可能になったオブジェクトがマークされます。
3.  **スタックへのランタイム書き込み**: バッファなしチャネルへの送信など、様々なランタイム操作がスタックの任意の場所（最低位の未ヒットスタックバリアより上を含む）に書き込む可能性があります。これには2つの解決策があります。多くの場合、ランタイムはケース2と同様に明示的なライトバリア操作を実行できます。しかし、バルクメモリ移動（typedmemmove）の場合、ランタイムはコピーされるメモリのポインタビットマップにすぐにアクセスできないため、単にデスティネーションより下のスタックバリアをアンワインドします。

**注意点 (Gotchas)**:

*   スタックを検査または操作するものは、スタックバリアを理解する必要があります。例えば、`gentraceback` はスタックバリアトランポリンに遭遇した際に元のリターンPCを使用する必要があります。`panic/recover` のようなスタックをアンワインドするものは、スタックのアンワインドと同時にスタックバリアもアンワインドする必要があります。
*   スタックバリアは、スタックがスキャンされたすべてのゴルーチンがライトバリアを実行することを要求します。Goは、並行スキャンフェーズ中にライトバリアをグローバルに有効にすることでこれを解決します。しかし、伝統的に、このフェーズではライトバリアは有効になっていませんでした。

## コアとなるコードの変更箇所

`src/runtime/mstkbar.go` ファイルに、スタックバリアの動作に関する詳細なコメントブロックが追加されました。これは、Goのガベージコレクタにおけるスタックバリアの役割、実装方法、およびその正確性を保証するためのメカニズムについて説明しています。

```diff
--- a/src/runtime/mstkbar.go
+++ b/src/runtime/mstkbar.go
@@ -3,6 +3,105 @@
 // license that can be found in the LICENSE file.
 
 // Garbage collector: stack barriers
+//
+// Stack barriers enable the garbage collector to determine how much
+// of a gorountine stack has changed between when a stack is scanned
+// during the concurrent scan phase and when it is re-scanned during
+// the stop-the-world mark termination phase. Mark termination only
+// needs to re-scan the changed part, so for deep stacks this can
+// significantly reduce GC pause time compared to the alternative of
+// re-scanning whole stacks. The deeper the stacks, the more stack
+// barriers help.
+//
+// When stacks are scanned during the concurrent scan phase, the stack
+// scan installs stack barriers by selecting stack frames and
+// overwriting the saved return PCs (or link registers) of these
+// frames with the PC of a "stack barrier trampoline". Later, when a
+// selected frame returns, it "returns" to this trampoline instead of
+// returning to its actual caller. The trampoline records that the
+// stack has unwound past this frame and jumps to the original return
+// PC recorded when the stack barrier was installed. Mark termination
+// re-scans only as far as the first frame that hasn't hit a stack
+// barrier and then removes and un-hit stack barriers.
+//
+// This scheme is very lightweight. No special code is required in the
+// mutator to record stack unwinding and the trampoline is only a few
+// assembly instructions.
+//
+// Book-keeping
+// ------------
+//
+// The primary cost of stack barriers is book-keeping: the runtime has
+// to record the locations of all stack barriers and the original
+// return PCs in order to return to the correct caller when a stack
+// barrier is hit and so it can remove un-hit stack barriers. In order
+// to minimize this cost, the Go runtime places stack barriers in
+// exponentially-spaced frames, starting 1K past the current frame.
+// The book-keeping structure hence grows logarithmically with the
+// size of the stack and mark termination re-scans at most twice as
+// much stack as necessary.
+//
+// The runtime reserves space for this book-keeping structure at the
+// top of the stack allocation itself (just above the outermost
+// frame). This is necessary because the regular memory allocator can
+// itself grow the stack, and hence can't be used when allocating
+// stack-related structures.
+//
+// For debugging, the runtime also supports installing stack barriers
+// at every frame. However, this requires significantly more
+// book-keeping space.
+//
+// Correctness
+// -----------
+//
+// The runtime and the compiler cooperate to ensure that all objects
+// reachable from the stack as of mark termination are marked.
+// Anything unchanged since the concurrent scan phase will be marked
+// because it is marked by the concurrent scan. After the concurrent
+// scan, there are three possible classes of stack modifications that
+// must be tracked:
+//
+// 1) Mutator writes below the lowest un-hit stack barrier. This
+// includes all writes performed by an executing function to its own
+// stack frame. This part of the stack will be re-scanned by mark
+// termination, which will mark any objects made reachable from
+// modifications to this part of the stack.
+//
+// 2) Mutator writes above the lowest un-hit stack barrier. It's
+// possible for a mutator to modify the stack above the lowest un-hit
+// stack barrier if a higher frame has passed down a pointer to a
+// stack variable in its frame. This is called an "up-pointer". The
+// compiler ensures that writes through up-pointers have an
+// accompanying write barrier (it simply doesn't distinguish between
+// writes through up-pointers and writes through heap pointers). This
+// write barrier marks any object made reachable from modifications to
+// this part of the stack.
+//
+// 3) Runtime writes to the stack. Various runtime operations such as
+// sends to unbuffered channels can write to arbitrary parts of the
+// stack, including above the lowest un-hit stack barrier. We solve
+// this in two ways. In many cases, the runtime can perform an
+// explicit write barrier operation like in case 2. However, in the
+// case of bulk memory move (typedmemmove), the runtime doesn't
+// necessary have ready access to a pointer bitmap for the memory
+// being copied, so it simply unwinds any stack barriers below the
+// destination.
+//
+// Gotchas
+// -------
+//
+// Anything that inspects or manipulates the stack potentially needs
+// to understand stack barriers. The most obvious case is that
+// gentraceback needs to use the original return PC when it encounters
+// the stack barrier trampoline. Anything that unwinds the stack such
+// as panic/recover must unwind stack barriers in tandem with
+// unwinding the stack.
+//
+// Stack barriers require that any goroutine whose stack has been
+// scanned must execute write barriers. Go solves this by simply
+// enabling write barriers globally during the concurrent scan phase.
+// However, traditionally, write barriers are not enabled during this
+// phase.
```

## コアとなるコードの解説

このコミットで追加されたコードは、Goランタイムの `src/runtime/mstkbar.go` ファイル内のコメントブロックです。このコメントは、スタックバリアの概念、その実装の詳細、およびGoのガベージコレクタにおけるその重要性を包括的に説明しています。

具体的には、以下の主要な側面が解説されています。

*   **目的**: スタックバリアが、並行スキャンフェーズとSTWマーク終了フェーズの間でスタックの変更を追跡し、STWポーズ時間を短縮するためにどのように機能するか。
*   **メカニズム**:
    *   並行スキャン中に、選択されたスタックフレームのリターンPCがスタックバリアトランポリンのアドレスで上書きされること。
    *   フレームがリターンする際にトランポリンにジャンプし、スタックのアンワインドを記録し、元のリターンPCに戻ること。
    *   STWマーク終了時に、未ヒットのバリアまでのみ再スキャンされること。
*   **軽量性**: ミューテータ側の特別なコードが不要であり、トランポリンが少数のアセンブリ命令で構成されること。
*   **簿記**: スタックバリアの位置と元のリターンPCを記録するための簿記の必要性、およびそのコストを最小限に抑えるための指数関数的な間隔でのバリア配置。簿記構造のためのスペースがスタックアロケーションの最上部に予約される理由。
*   **正確性**: ランタイムとコンパイラが協力して、スタックから到達可能なすべてのオブジェクトがマークされることを保証する方法。特に、ミューテータによるスタックへの書き込み（最低位のバリアより下、およびアップポインタを介したバリアより上）、およびランタイムによるスタックへの書き込み（ライトバリアまたはバリアのアンワインド）がどのように処理されるか。
*   **注意点**: スタックを操作する他の機能（`gentraceback`、`panic/recover`）がスタックバリアを理解する必要があること。並行スキャンフェーズ中にライトバリアがグローバルに有効になることの必要性。

このコメントブロックは、Goのガベージコレクタの内部動作、特にスタック管理とポーズ時間の最適化に関心のある開発者にとって、非常に価値のあるドキュメンテーションです。

## 関連リンク

*   Goのガベージコレクションに関する公式ドキュメントやブログ記事:
    *   [Go's new GC: A look inside - The Go Programming Language](https://go.dev/blog/go15gc) (Go 1.5でのGC改善に関する記事で、スタックバリアの背景となる情報が含まれている可能性があります)
    *   [Go GC: Prioritizing low latency and simplicity](https://go.dev/blog/go15gc-slides) (Go 1.5 GCの設計思想に関するスライド)

## 参考にした情報源リンク

*   Goのソースコード: `src/runtime/mstkbar.go`
*   Goの公式ブログ: 上記の関連リンクに挙げたGo 1.5 GCに関する記事。
*   Goのレビューシステム: `https://go-review.googlesource.com/14051` (このコミットのレビューページ)
*   Goのガベージコレクションに関する一般的な知識。
*   コンパイラとランタイムにおけるスタック管理の一般的な概念。
*   ライトバリアの概念。
